<p class="help-block small text-muted">
  You can use <strong>some markdown</strong>.<br>
  Italics (*), bold (**), inline code (`), code blocks without newlines (```), &lt;kbd&gt;, and blockquotes (>) are supported.
</p>
